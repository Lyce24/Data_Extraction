#include <math.h>
#include <iostream>

using namespace std;

int main()
{
    int a = 4;
    int b = pow(4,2);
    int result = sqrt(a);
    cout << b << endl;
    cout << result << endl;
    return result;
}